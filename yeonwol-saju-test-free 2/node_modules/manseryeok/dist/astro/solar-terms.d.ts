/**
 * 24절기(節氣) 계산.
 *
 * 절기 인덱스는 0=소한 … 23=동지 순서이며, 각 인덱스의 황경은 (285 + 15*index) mod 360.
 * 짝수 인덱스가 '절(節)'로 월(月)의 시작을 결정한다 (예: index 2 = 입춘 = 인월 시작).
 */
import type { SolarTerm } from '../types';
/** 절기 이름 (한글) — index 0 = 소한 */
export declare const SOLAR_TERM_NAMES: readonly ["소한", "대한", "입춘", "우수", "경칩", "춘분", "청명", "곡우", "입하", "소만", "망종", "하지", "소서", "대서", "입추", "처서", "백로", "추분", "한로", "상강", "입동", "소설", "대설", "동지"];
/** 절기 이름 (한자) */
export declare const SOLAR_TERM_NAMES_HANJA: readonly ["小寒", "大寒", "立春", "雨水", "驚蟄", "春分", "淸明", "穀雨", "立夏", "小滿", "芒種", "夏至", "小暑", "大暑", "立秋", "處暑", "白露", "秋分", "寒露", "霜降", "立冬", "小雪", "大雪", "冬至"];
/**
 * 특정 연도·절기의 절입 절대 순간(UTC ms)을 구한다.
 * @param year 양력 연도 (절기가 속한 달력상 연도)
 * @param index 절기 인덱스 (0~23)
 */
export declare function solarTermInstantMs(year: number, index: number): number;
/**
 * 절입 시각을 반환한다.
 * `date` 는 절입의 절대 순간(UTC 기준 Date)이다. KST 로 표기하려면
 * `date.toLocaleString('ko-KR', { timeZone: 'Asia/Seoul' })` 등을 사용한다.
 *
 * 1800~2300년은 정밀 절입표로 분 단위 정확하며, 범위 밖 연도는 Meeus 근사로
 * 폴백되어 수 분 이상(연도가 멀수록 증가)의 오차가 있을 수 있다.
 */
export declare function getSolarTerm(year: number, index: number): SolarTerm;
/**
 * 한 해의 24절기를 모두 반환한다 (시간 순서).
 */
export declare function getSolarTermsOfYear(year: number): SolarTerm[];
/**
 * 주어진 절대 순간(UTC ms)이 입춘 이후인지 판정하여, 사주 연도 보정에 쓸 연도를 반환한다.
 * 입춘 이전이면 year-1, 이후면 year.
 */
export declare function sajuYearForInstant(instantMs: number, calendarYear: number): number;
/**
 * 절대 순간(UTC ms)에 해당하는 절기 월 번호(1=인월 … 12=축월)를 반환한다.
 * 정밀 절입표를 기준으로, 해당 순간 직전의 절(節)이 시작한 월을 택한다.
 */
export declare function sajuMonthForInstant(instantMs: number): number;
